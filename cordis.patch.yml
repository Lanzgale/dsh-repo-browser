# dsh-repo-browser bundle patch: mounts the repo-browser plugin into the
# host composition of a dsh profile. The host half registers the
# /plugins/repo-browser/* HTTP routes (list / groups) and reads the
# GitHub token from the DSH credentials library; the web client
# (dsh.client entry) renders the right-side repo-browser panel in
# shell.overlay plus the session-header toggle button.
#
# Install: `dsh plugin --profile <name> add <this package>` (npm or a local
# path). The `dsh plugin` command pnpm-installs the package into the profile
# and reconciles it into the profile's `dsh.profile.bundles` layer list.
- insert:
    - id: repo-browser
      name: dsh-repo-browser
